(print! nil)
(print! nil)
(print! t)
(print! (eq? 1 1))
(diagnostics!)
(defun! bif (x)
  (cons x x))
(diagnostics!)
(setq! foo (print! (bif (bif (bif 67)))))
(print! (eq? (car foo) (cdr foo)))
(diagnostics!)
(print! foo)
