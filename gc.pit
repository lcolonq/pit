(diagnostics!)
(setq! foo (cons 1 2))
(setq! bar (cons foo 3))
(setq! baz (cons bar foo))
(diagnostics!)
(print! foo)
(setcar! foo baz)
(print! (cdr (car (car foo))))
(diagnostics!)
(setq! foo nil)
(setq! bar nil)
(setq! baz nil)
(diagnostics!)
